import {FullReachDirective} from './full-reach.directive';

describe('FullReachDirective', () => {
  it('should create an instance', () => {
    const directive = new FullReachDirective();
    expect(directive).toBeTruthy();
  });
});
