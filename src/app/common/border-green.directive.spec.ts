import {BorderGreenDirective} from './border-green.directive';

describe('BorderGreenDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderGreenDirective();
    expect(directive).toBeTruthy();
  });
});
