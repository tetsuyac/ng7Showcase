import {NudgeRimDirective} from './nudge-rim.directive';

describe('NudgeRimDirective', () => {
  it('should create an instance', () => {
    const directive = new NudgeRimDirective();
    expect(directive).toBeTruthy();
  });
});
