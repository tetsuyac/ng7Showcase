import { RimSpaceDirective } from './rim-space.directive';

describe('RimSpaceDirective', () => {
  it('should create an instance', () => {
    const directive = new RimSpaceDirective();
    expect(directive).toBeTruthy();
  });
});
